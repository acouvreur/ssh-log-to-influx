apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    database: telegraf
    user: telegraf
    password: grafana
    url: http://influxdb:8086
    isDefault: true
    jsonData:
      httpMode: GET